class InterviewQuestionResponse {
  int id;
  int topicId;
  int subjectId;
  String question;
  String answer;

  InterviewQuestionResponse(
      {this.id, this.topicId, this.subjectId, this.question, this.answer});
}
